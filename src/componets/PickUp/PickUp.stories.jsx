import { fn } from "storybook/test";
import { PickUp } from "./PickUp";

export default {
  title: "Components/PickUp",
  component: PickUp,
};

export const Default = {
  render: () => <PickUp />,
};
